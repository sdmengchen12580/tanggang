<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 是否是圆角 -->
    <corners android:bottomLeftRadius="20dp" />

    <stroke
        android:width="1px"
        android:color="@color/popup_btn_stroke"

        />
    <!-- shape的颜色，支持渐变 -->
    <gradient
        android:angle="270"
        android:centerColor="@color/popup_btn_gradient"
        android:centerY="0.45"
        android:endColor="@color/popup_btn_gradient"
        android:startColor="@color/popup_btn_gradient" />

</shape>