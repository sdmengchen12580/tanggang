<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 是否是圆角 -->
    <corners android:radius="5dip" />

    <stroke
        android:width="1px"
        android:color="@color/qr_school_url_btn_normal"
        />
    <!-- shape的颜色，支持渐变 -->
    <gradient
        android:angle="270"
        android:centerColor="@color/qr_school_url_btn_normal"
        android:centerY="0.45"
        android:endColor="@color/qr_school_url_btn_normal"
        android:startColor="@color/qr_school_url_btn_normal" />

</shape>