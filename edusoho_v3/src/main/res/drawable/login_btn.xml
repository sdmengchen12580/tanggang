<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 是否是圆角 -->
    <corners android:radius="3dip" />
    
    <stroke
        android:color="@color/login_btn_gradient"
	    android:width="1px"
	    />
    <!-- shape的颜色，支持渐变 -->
    <gradient
        android:angle="270"
        android:centerColor="@color/login_btn_gradient"
        android:centerY="0.45"
        android:endColor="@color/login_btn_gradient"
        android:startColor="@color/login_btn_gradient" />

</shape>