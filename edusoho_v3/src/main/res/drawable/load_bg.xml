<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 是否是圆角 -->
    <corners android:radius="8px" />
    <stroke
        android:width="1px"
        android:color="@color/load_bg_stroke" />
    <!-- shape的颜色，支持渐变 -->
    <gradient
        android:centerColor="@color/load_bg"
        android:endColor="@color/load_bg"
        android:startColor="@color/load_bg" />

</shape>